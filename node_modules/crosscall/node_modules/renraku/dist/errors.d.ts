export declare class RenrakuApiError extends Error {
    readonly code: number;
    readonly name: string;
    constructor(code: number, message: string);
}
export declare const err: (code: number, message: string) => RenrakuApiError;
