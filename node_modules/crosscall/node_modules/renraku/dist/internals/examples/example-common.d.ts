import { ApiShape } from "../../types.js";
export interface NuclearMeta {
    accessToken: string;
}
export interface NuclearMetaPayload {
    userId: string;
}
export declare const makeNuclearApi: () => {
    reactor: import("../../types.js").ProcessPayloadTopic<NuclearMeta, {
        generatePower(payload: NuclearMetaPayload, a: number, b: number): Promise<number>;
        radioactiveMeltdown(payload: NuclearMetaPayload): Promise<never>;
    }>;
};
export declare type NuclearApi = ReturnType<typeof makeNuclearApi>;
export declare const nuclearShape: ApiShape<NuclearApi>;
