import { SignatureSignOptions } from "redcrypto/dist/types.js";
export interface Order {
    topic: string;
    func: string;
    params: any[];
}
export declare type SignatureSign = (options: SignatureSignOptions) => string;
