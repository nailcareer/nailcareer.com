import { WhitelistPermissions } from "../../types.js";
export declare function verifyWhitelist({ id, body, whitelist, signature }: {
    id: string;
    body: string;
    signature: string;
    whitelist: WhitelistPermissions;
}): Promise<boolean>;
