import { ServerApi, Logger, Headers } from "../../types.js";
export declare function apiCall<A extends ServerApi>({ id, debug, logger, expose, headers, body, }: {
    expose: A;
    id: string;
    body: string;
    debug: boolean;
    logger: Logger;
    headers: Headers;
}): Promise<any>;
