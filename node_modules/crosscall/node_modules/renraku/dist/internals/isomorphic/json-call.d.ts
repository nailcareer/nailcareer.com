import { Headers } from "../../types.js";
export declare function jsonCall<Data, Ret>({ url, data, fetch, headers, }: {
    data: Data;
    url: string;
    headers: Headers;
    fetch: typeof window.fetch;
}): Promise<Ret>;
