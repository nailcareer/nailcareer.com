import { ClientApi, ClientOptions } from "../../types.js";
export declare const prepareApiClient: ({ fetch }: {
    fetch: ((input: RequestInfo, init?: RequestInit) => Promise<Response>) & typeof fetch;
}) => <A extends ClientApi>({ url, shape, }: ClientOptions<A>) => A;
