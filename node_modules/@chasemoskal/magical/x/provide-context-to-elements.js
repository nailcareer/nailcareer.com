export function provideContextToElements(context, elements) {
    const newElements = {};
    for (const [key, Element] of Object.entries(elements))
        newElements[key] = Element.withContext(context);
    return newElements;
}
//# sourceMappingURL=provide-context-to-elements.js.map