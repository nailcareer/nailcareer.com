var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _SlateGold_state;
import { html, css } from "lit";
import { flat } from "../../nexus/state.js";
import { GoldElement } from "../../element/gold.js";
export class SlateGold extends GoldElement {
    constructor() {
        super(...arguments);
        _SlateGold_state.set(this, flat.state({
            count: 0,
        }));
    }
    static get styles() { return css `span {color: orange}`; }
    render() {
        return html `
			<span>${__classPrivateFieldGet(this, _SlateGold_state, "f").count}</span>
			<button @click=${() => __classPrivateFieldGet(this, _SlateGold_state, "f").count++}>gold</button>
		`;
    }
}
_SlateGold_state = new WeakMap();
//# sourceMappingURL=slate-gold.js.map