export type MapSubset<K, V> = {
    has(key: K): boolean;
    get(key: K): V | undefined;
    set(key: K, value: V): void;
};
export declare function maptool<K, V>(map: MapSubset<K, V>): {
    guarantee: (key: K, make: () => V) => V;
};
