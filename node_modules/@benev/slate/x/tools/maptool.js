export function maptool(map) {
    return {
        guarantee: (key, make) => (mapGuarantee(map, key, make)),
    };
}
function mapGuarantee(map, key, make) {
    if (map.has(key))
        return map.get(key);
    else {
        const value = make();
        map.set(key, value);
        return value;
    }
}
//# sourceMappingURL=maptool.js.map