import { Op } from "./op.js";
export function prep_render_op({ loading, error }) {
    return function render_op(op, on_ready) {
        return Op.select(op, {
            loading,
            error,
            ready: on_ready,
        });
    };
}
//# sourceMappingURL=prep_render_op.js.map