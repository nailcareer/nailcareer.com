import { Signal } from "../signals/signal.js";
import { html as lit_html } from "lit";
export { css, svg } from "lit";
export const html = (strings, ...values) => (lit_html(strings, ...values.map(Signal.unwrap)));
//# sourceMappingURL=html.js.map