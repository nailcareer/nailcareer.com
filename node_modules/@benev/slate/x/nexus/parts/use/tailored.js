import { Use } from "./parts/use.js";
import { UseGold } from "./parts/use_gold.js";
import { UseShadow } from "./parts/use_shadow.js";
import { UseSilver } from "./parts/use_silver.js";
export class UseShadowComponent extends UseGold {
}
export class UseLightComponent extends UseSilver {
}
export class UseLightView extends Use {
    name(name) {
        this.once(() => this.element.setAttribute("view", name));
    }
    constructor(element, rerender, context) {
        super(rerender, context);
        this.element = element;
    }
}
export class UseShadowView extends UseShadow {
    name(name) {
        this.once(() => this.element.setAttribute("view", name));
    }
}
//# sourceMappingURL=tailored.js.map