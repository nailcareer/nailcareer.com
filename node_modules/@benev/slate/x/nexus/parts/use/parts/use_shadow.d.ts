import { CSSResultGroup } from "lit";
import { Use } from "./use.js";
import { Context } from "../../../context.js";
export declare class UseShadow<C extends Context = Context, E extends HTMLElement = HTMLElement> extends Use<C> {
    #private;
    get element(): E;
    get shadow(): ShadowRoot;
    styles(styles: CSSResultGroup | undefined): void;
    constructor(element: E, shadow: ShadowRoot, rerender: () => void, context: C);
}
