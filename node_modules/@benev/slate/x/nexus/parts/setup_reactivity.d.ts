import { TemplateResult } from "lit";
export type Reactivity<P extends any[]> = {
    render: (...props: P) => (TemplateResult | void);
    stop: () => void;
};
export declare function setup_reactivity<P extends any[]>(render: (...props: P) => (TemplateResult | void), rerender: () => void): Reactivity<P>;
