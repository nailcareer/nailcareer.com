import * as state from "../../nexus/state.js";
import { mixin } from "./mixin.js";
import { ob } from "../../tools/ob.js";
import { Pipe } from "../../tools/pipe.js";
export var apply;
(function (apply) {
    apply.css = ((theme) => ((elements) => (ob(elements).map(Element => mixin.css(theme)(Element)))));
    apply.flat = ((flat) => ((elements) => (ob(elements).map((Element) => mixin.flat(flat)(Element)))));
    apply.signals = ((signals) => ((elements) => (ob(elements).map((Element) => mixin.signals(signals)(Element)))));
    apply.reactor = ((r = state.reactor) => ((elements) => (ob(elements).map((Element) => mixin.reactor(r)(Element)))));
    apply.context = ((context) => ((elements) => Pipe.with(elements)
        .to(apply.css(context.theme))
        .to(apply.reactor())
        .done()));
})(apply || (apply = {}));
//# sourceMappingURL=apply.js.map