import { Country } from "./types/country.js";
export declare class CountryLibrary extends Map<string, Country> {
    constructor();
    query(two_letter_codes: string[]): {
        known: Country[];
        unknown: string[];
    };
    query_names(two_letter_codes: string[]): string[];
}
