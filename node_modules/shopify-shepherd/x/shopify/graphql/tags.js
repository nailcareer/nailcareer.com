import { gql } from "../../tools/gql.js";
import { paginated_node } from "./units/paginated.js";
export function make_request_for_tags({ page_size }) {
    return {
        query: gql `
			query FetchTags($first: Int!) {
				productTags(first: $first) {
					${paginated_node()}
				}
			}
		`,
        variables: {
            first: page_size,
        },
    };
}
//# sourceMappingURL=tags.js.map