import { GqlEdges } from "./edges.js";
export declare const paginated: (node: string) => string;
export declare const paginated_node: () => string;
export type GqlPaginated<N> = GqlEdges<N> & {
    pageInfo: {
        hasNextPage: boolean;
        endCursor: string;
    };
};
