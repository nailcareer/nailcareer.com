import { gql } from "../../tools/gql.js";
import { product } from "./units/product.js";
import { paginated } from "./units/paginated.js";
export function make_request_for_products({ query, after, image_format, page_size, sort, reverse, }) {
    return {
        query: gql `
			query FetchProducts(
				$first: Int!,
				$after: String,
				$sort: ProductSortKeys,
				$reverse: Boolean,
				$query: String,
				) {
				products(
					first: $first,
					after: $after,
					sortKey: $sort,
					reverse: $reverse,
					query: $query,
					) {
					${paginated(product({ image_format }))}
				}
			}
		`,
        variables: {
            query,
            after,
            first: page_size,
            sort,
            reverse,
        },
    };
}
//# sourceMappingURL=products.js.map