import { gql } from "../../tools/gql.js";
import { paginated } from "./units/paginated.js";
import { image } from "./units/image.js";
export function make_request_for_collections({ after, image_format, page_size, }) {
    return {
        query: gql `
			query FetchCollections($first: Int!, $after: String) {
				collections(first: $first, after: $after) {
					${paginated(gql `

						description
						descriptionHtml
						handle
						id
						title
						updatedAt
						onlineStoreUrl

						image {
							${image(image_format)}
						}

					`)}
				}
			}
		`,
        variables: {
            after,
            first: page_size,
        },
    };
}
//# sourceMappingURL=collections.js.map