import { gql } from "../../tools/gql.js";
import { product } from "./units/product.js";
export function make_request_for_product_recommendations({ intent, product_id, image_format, }) {
    return {
        query: gql `
			query FetchProductRecommendations($product_id: ID!, $intent: ProductRecommendationIntent) {
				productRecommendations(productId: $product_id, intent: $intent) {
					${product({ image_format })}
				}
			}
		`,
        variables: {
            intent,
            product_id,
        },
    };
}
//# sourceMappingURL=product_recommendations.js.map