import { querytext } from "./utils/querytext.js";
export function convert_product_query_spec_to_string({ tags = [], terms = [], } = {}) {
    const segments = [];
    if (tags.length)
        segments.push(tags
            .map(tag => querytext `(tag:${tag})`)
            .join(" AND "));
    if (terms.length)
        segments.push(terms
            .map(term => querytext `(title:*${term}* OR tag:*${term}*)`)
            .join(" AND "));
    return segments.length
        ? segments
            .map(segment => `(${segment})`)
            .join(" AND ")
        : undefined;
}
//# sourceMappingURL=convert_product_query_spec_to_string.js.map