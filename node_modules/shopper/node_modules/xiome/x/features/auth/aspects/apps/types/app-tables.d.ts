import * as dbmage from "dbmage";
export declare type AppSchema = dbmage.AsSchema<{
    registrations: AppRegistrationRow;
    owners: OwnerRow;
}>;
export declare type AppRegistrationRow = dbmage.AsRow<{
    appId: dbmage.Id;
    label: string;
    home: string;
    origins: string;
    archived: boolean;
}>;
export declare type OwnerRow = dbmage.AsRow<{
    appId: dbmage.Id;
    userId: dbmage.Id;
}>;
