import { Id } from "dbmage";
import { StoreLinkedAuth } from "../../../types/store-metas-and-auths.js";
export declare const helpersForManagingSubscriptions: ({ database, stripeAccountId, stripeLiaisonAccount, generateId, }: StoreLinkedAuth & {
    generateId: () => Id;
}) => {
    createStripeProductAndPrice(options: {
        planLabel: string;
        tierPrice: number;
        tierCurrency: string;
        tierInterval: "month" | "year";
    }): Promise<{
        stripeProductId: string;
        stripePriceId: string;
    }>;
    createPlanAndTier({ planLabel, tierLabel, stripePriceId, stripeProductId, }: {
        planLabel: string;
        tierLabel: string;
        stripePriceId: string;
        stripeProductId: string;
    }): Promise<{
        planId: Id;
        tierId: Id;
        time: number;
    }>;
};
