import { getStripeId } from "./get-stripe-id.js";
export const toSubscriptionDetails = (subscription) => ({
    id: subscription.id,
    status: subscription.status,
    current_period_end: subscription.current_period_end,
    productIds: subscription.items.data
        .map(data => getStripeId(data.price.product)),
});
//# sourceMappingURL=to-subscription-details.js.map