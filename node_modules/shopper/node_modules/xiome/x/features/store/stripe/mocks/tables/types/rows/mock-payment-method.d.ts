import { Stripe } from "stripe";
import { FlexibleRow } from "./custom-db/flexible-row.js";
export declare type MockPaymentMethod = FlexibleRow<{
    id: string;
    card: Stripe.PaymentMethod.Card;
} & Partial<Stripe.PaymentMethod>>;
