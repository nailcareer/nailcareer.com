import { SecretConfig } from "../types/secret-config.js";
export declare const mockConfig: ({ platformHome, platformOrigins }: {
    platformHome: string;
    platformOrigins: string[];
}) => SecretConfig;
