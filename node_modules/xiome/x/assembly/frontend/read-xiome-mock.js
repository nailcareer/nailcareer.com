import { configReading } from "./tools/config-reading.js";
export function readXiomeMock() {
    const { attr } = configReading("xiome-mock");
    const rawMode = attr("mode");
    return {
        mode: (rawMode === undefined
            ? undefined
            : rawMode === "platform"
                ? "platform"
                : "app")
    };
}
//# sourceMappingURL=read-xiome-mock.js.map