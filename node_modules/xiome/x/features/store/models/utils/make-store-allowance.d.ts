import { makeStoreState } from "../state/make-store-state.js";
export declare function makeStoreAllowance(state: ReturnType<typeof makeStoreState>): {
    readonly manageStore: boolean;
    readonly connectStripeAccount: boolean;
    readonly giveAwayFreebies: boolean;
};
