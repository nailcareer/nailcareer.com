import * as renraku from "renraku";
import { StoreMeta } from "../../types/store-metas-and-auths.js";
import { StoreServiceOptions, SubscriptionPlan } from "../../types/store-concepts.js";
export declare const makeSubscriptionPlanningService: (options: StoreServiceOptions) => renraku.Service<StoreMeta, import("../../types/store-metas-and-auths.js").StoreLinkedAuth, {
    listSubscriptionPlans(): Promise<SubscriptionPlan[]>;
    addPlan(inputs: {
        planLabel: string;
        tierLabel: string;
        tierPrice: number;
    }): Promise<SubscriptionPlan>;
    addTier({}: {
        label: string;
        price: number;
        planId: string;
    }): Promise<any>;
    editPlan(): Promise<void>;
    editTier(): Promise<void>;
}>;
