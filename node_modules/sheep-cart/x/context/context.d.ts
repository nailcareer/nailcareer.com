import { GqlCollection, GqlTag, Shopify } from "shopify-shepherd";
import { Op, CSSResultGroup, Context as SlateContext } from "@benev/slate";
import { Cart } from "../carting/cart.js";
import { Route } from "../routing/types.js";
import { Modal } from "../modaling/modal.js";
import { Router } from "../routing/router.js";
import { Situation } from "./types/situations.js";
import { State } from "./parts/init_state.js";
import { CartStore } from "../carting/parts/cart_store.js";
export declare class Context extends SlateContext {
    #private;
    shopify: Shopify;
    router: Router;
    theme: CSSResultGroup;
    readonly state: State;
    cart: Cart;
    modal: Modal;
    constructor(shopify: Shopify, router: Router, theme: CSSResultGroup, cart_store: CartStore);
    set_route: (route: Route) => Route;
    set_tags: (tags: GqlTag[]) => string[];
    set_collections: (collections: GqlCollection[]) => GqlCollection[];
    set_situation_op: (op: Op.For<Situation.Whatever>) => Op.For<Situation.Whatever>;
    toggle_cart_open: (open?: boolean) => void;
}
