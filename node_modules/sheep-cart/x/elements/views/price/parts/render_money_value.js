import { html } from "@benev/slate";
export function render_money_value({ symbol, currency, big, small }) {
    return html `
		${symbol
        ? html `<span class=symbol>${symbol}</span>`
        : undefined}
		<span class=big>${big}</span>
		<span class=stack>
			${small
        ? html `<span class=small>${small}</span>`
        : undefined}
			<span class=currency>${currency}</span>
		</span>
	`;
}
//# sourceMappingURL=render_money_value.js.map