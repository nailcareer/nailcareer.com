export function featured_thumbnail(product) {
    const imageEdge = product.images.edges
        .find(n => { var _a; return n.node.id === ((_a = product.featuredImage) === null || _a === void 0 ? void 0 : _a.id); });
    return imageEdge === null || imageEdge === void 0 ? void 0 : imageEdge.node.url_tiny;
}
//# sourceMappingURL=featured_thumbnail.js.map