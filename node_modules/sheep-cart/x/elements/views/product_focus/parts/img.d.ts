import { GqlImage } from "shopify-shepherd";
import { Img } from "./types.js";
export declare const img: {
    tiny: (image?: GqlImage) => Img;
    small: (image?: GqlImage) => Img;
    medium: (image?: GqlImage) => Img;
    large: (image?: GqlImage) => Img;
    original: (image?: GqlImage) => Img;
};
