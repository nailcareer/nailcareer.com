import { html } from "@benev/slate";
export function render_img({ img, part = "", onclick = () => { }, }) {
    return html `
		<img
			alt="${img.alt}"
			src="${img.src}"
			part="${part}"
			@click=${(event) => onclick(event, img)}
			/>
	`;
}
//# sourceMappingURL=render_img.js.map