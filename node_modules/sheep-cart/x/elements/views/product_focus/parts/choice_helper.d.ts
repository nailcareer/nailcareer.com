import { GqlImage } from "shopify-shepherd";
import { Choice } from "./types.js";
import { ProductHelper } from "./product_helper.js";
export declare class ChoiceHelper {
    readonly productHelper: ProductHelper;
    readonly choices: Choice[];
    constructor(productHelper: ProductHelper, choices: Choice[]);
    get_choice(name: string): string;
    get selected_variant(): import("shopify-shepherd").GqlVariant;
    get chosen_image(): GqlImage | undefined;
    get side_images(): GqlImage[];
}
