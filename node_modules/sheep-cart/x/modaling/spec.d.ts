export declare namespace ModalSpec {
    type Kind = "confirm" | "image" | "cart";
    interface Base {
        kind: Kind;
    }
    interface Image extends Base {
        kind: "image";
        img: {
            src: string;
            alt: string;
        };
    }
    interface Cart extends Base {
        kind: "cart";
    }
    type Whatever = Image | Cart;
}
