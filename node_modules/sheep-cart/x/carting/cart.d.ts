import { GqlProduct, Shopify } from "shopify-shepherd";
import { CartStore } from "./parts/cart_store.js";
import { CartState, CartUnit } from "./parts/types.js";
export declare class Cart {
    #private;
    readonly state: CartState;
    constructor(shopify: Shopify, store: CartStore);
    save(): void;
    load(): Promise<void>;
    get units(): CartUnit[];
    add(variant_id: string, product: GqlProduct): void;
    remove(variant_id: string): void;
    set_quantity(variant_id: string, quantity: number): void;
    clear(): void;
}
