import { CartStoreData } from "./types.js";
export declare class CartStore {
    #private;
    constructor(key: string, storage: Storage);
    save(pack: CartStoreData.V1["pack"]): void;
    load(): {
        items: import("./types.js").CartItem[];
    };
}
