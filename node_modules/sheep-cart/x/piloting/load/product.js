import { Op } from "@benev/slate";
import { ShopifyNotFoundError } from "shopify-shepherd";
export async function load_single_product(set_situation_op, product) {
    await Op.run(set_situation_op, async () => {
        try {
            return {
                type: "single_product",
                product: await product,
            };
        }
        catch (error) {
            if (error instanceof ShopifyNotFoundError)
                return {
                    type: "not_found",
                    message: "Product not found",
                };
            else
                throw error;
        }
    });
}
//# sourceMappingURL=product.js.map