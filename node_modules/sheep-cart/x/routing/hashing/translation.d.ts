import { HomeArea, Route } from "../types.js";
export declare class Translation {
    #private;
    constructor(home: HomeArea, prefix: string);
    routify(hash: string): Route;
    hashify(route: Route): string;
}
