import { HomeArea, Route, RouterOptions } from "./types.js";
export declare class Router {
    #private;
    on_route_change: import("@benev/slate").Pub<Route>;
    constructor({ home, prefix, set_hash }: RouterOptions);
    static setup(home: HomeArea, prefix?: string): Router;
    get route(): Route;
    set route(r: Route);
    apply_hash(hash: string): void;
    hashchange: (event: HashChangeEvent) => void;
    routes: {
        home: () => {
            url: string;
            go: () => void;
        };
        collections: () => {
            url: string;
            go: () => void;
        };
        products: () => {
            url: string;
            go: () => void;
        };
        search: (terms: string[], tags: string[]) => {
            url: string;
            go: () => void;
        };
        collection: ({ id, handle: label }: {
            id: string;
            handle: string;
        }) => {
            url: string;
            go: () => void;
        };
        product: ({ id, handle: label }: {
            id: string;
            handle: string;
        }) => {
            url: string;
            go: () => void;
        };
    };
    get search_terms(): string[];
    get search_tags(): string[];
}
