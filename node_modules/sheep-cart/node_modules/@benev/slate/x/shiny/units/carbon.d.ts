import { Shell } from "../shell.js";
import { Context } from "../context.js";
import { GoldElement } from "../../element/gold.js";
import { ShadowSettings, CarbonRenderer } from "../parts/types.js";
export declare const prepare_carbon: <C extends Context>(shell: Shell<C>) => (settings: ShadowSettings, renderer: CarbonRenderer<C>) => typeof GoldElement;
