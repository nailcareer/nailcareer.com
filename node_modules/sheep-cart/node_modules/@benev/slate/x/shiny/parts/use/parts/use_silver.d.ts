import { Use } from "./use.js";
import { Context } from "../../../context.js";
import { SilverElement } from "../../../../element/silver.js";
import { SetupAttrs } from "../../setup_use_attrs.js";
export declare class UseSilver<C extends Context = Context> extends Use<C> {
    #private;
    get element(): SilverElement;
    readonly attrs: SetupAttrs;
    constructor(element: SilverElement, rerender: () => void, context: C);
}
