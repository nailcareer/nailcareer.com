import { reactor } from "../state.js";
export function setup_reactivity(render, rerender) {
    const lean = reactor.lean(rerender);
    return {
        stop: lean.stop,
        render: (...props) => lean.collect(() => render(...props)),
    };
}
//# sourceMappingURL=setup_reactivity.js.map