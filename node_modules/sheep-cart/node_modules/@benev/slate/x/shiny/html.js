import { Signal } from "../signals/signal.js";
import { html as lit_html } from "lit";
export { svg, css } from "lit";
export const html = (strings, ...values) => (lit_html(strings, ...values.map(value => ((value instanceof Signal)
    ? value.value
    : value))));
//# sourceMappingURL=html.js.map