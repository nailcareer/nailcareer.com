import { SignalListener } from "./parts/listener.js";
import { accessed } from "./parts/accessed_symbol.js";
export declare class Signal<V> {
    #private;
    [accessed]: boolean;
    constructor(v: V);
    subscribe(listener: SignalListener<V>): () => undefined;
    once(listener: SignalListener<V>): () => undefined;
    clear(): void;
    publish(): Promise<void>;
    get wait(): Promise<V>;
    get value(): V;
    set value(s: V);
}
