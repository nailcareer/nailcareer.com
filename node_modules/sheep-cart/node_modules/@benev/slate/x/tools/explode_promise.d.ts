export type PromiseParts = ReturnType<typeof explode_promise>;
export declare function explode_promise<T>(): {
    promise: Promise<T>;
    resolve: (value: T) => void;
    reject: (reason: any) => void;
};
